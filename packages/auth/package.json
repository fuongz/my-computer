{
	"name": "@fuongz/auth",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"//exports": "TypeScript source, exported directly. Both consumers are bundlers (vite for the web app, wrangler/esbuild for the API) and bun resolves workspace symlinks natively, so a dist/ build step would only add an ordering constraint to `bun run --filter '*' build` for no gain.",
	"exports": {
		".": "./src/index.ts",
		"./schema": "./src/schema/index.ts"
	},
	"scripts": {
		"build": "echo '@fuongz/auth ships TypeScript source — nothing to build'",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@better-auth/api-key": "^1.6.26",
		"@better-auth/drizzle-adapter": "^1.6.26",
		"better-auth": "^1.6.26",
		"drizzle-orm": "^0.45.2"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^5.20260810.1",
		"typescript": "^6.0.3"
	}
}
